# CrowMods AI — Phase 154
## Automatic Policy Rollback

Return a policy to the last known-good version when configured health gates fail.

This package contains the Phase 154 implementation increment, API surface,
database audit schema, regression tests, smoke check, and documentation.

Security-sensitive deployments must put the endpoint behind the project's
authentication and authorization middleware.
