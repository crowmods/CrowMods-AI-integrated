# CrowMods AI — Phase 155
## Approval Conflict Resolution

Detect conflicting approval decisions and resolve them with deterministic, auditable precedence.

This package contains the Phase 155 implementation increment, API surface,
database audit schema, regression tests, smoke check, and documentation.

Security-sensitive deployments must put the endpoint behind the project's
authentication and authorization middleware.
