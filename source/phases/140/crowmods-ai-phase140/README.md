# CrowMods AI — Phase 140

Adds locked-row purge delete transactions, alert escalation-cap reset windows,
calibration binding CAS enforcement, and idempotent manifest retry/replay audit.

No credentials or secrets are included.
