# CrowMods AI — Phase 153
## Canary Policy Rollouts

Deploy policy versions to a bounded percentage of eligible traffic and expose rollout state safely.

This package contains the Phase 153 implementation increment, API surface,
database audit schema, regression tests, smoke check, and documentation.

Security-sensitive deployments must put the endpoint behind the project's
authentication and authorization middleware.
