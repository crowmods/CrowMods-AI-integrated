# CrowMods AI — Phase 142

Adds purge outcome transaction joins, alert transition history and
cooldown-aware recovery, lease validation inside atomic calibration commits,
and replay-cache conflict detection plus cleanup metrics.

No credentials or secrets are included.
