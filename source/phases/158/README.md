# CrowMods AI — Phase 158
## Replay Anomaly Detection

Detect unusual replay patterns using bounded rate and fingerprint signals.

This package contains the Phase 158 implementation increment, API surface,
database audit schema, regression tests, smoke check, and documentation.

Security-sensitive deployments must put the endpoint behind the project's
authentication and authorization middleware.
