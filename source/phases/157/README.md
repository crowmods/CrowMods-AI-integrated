# CrowMods AI — Phase 157
## Replay Fingerprint Registry

Persist normalized replay fingerprints for deterministic duplicate detection.

This package contains the Phase 157 implementation increment, API surface,
database audit schema, regression tests, smoke check, and documentation.

Security-sensitive deployments must put the endpoint behind the project's
authentication and authorization middleware.
